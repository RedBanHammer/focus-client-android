<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.usc.csci310.focus.focus">

    <uses-permission android:name="android.permission.GET_TASKS" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" />

    <service android:name=".NotificationBlockingService"
        android:label="@string/notification_service_label"
        android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
        <intent-filter>
            <action android:name="android.service.notification.NotificationListenerService" />
        </intent-filter>
    </service>

    <service
        android:name=".AppBlocker"
        android:exported="false"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.ProfileList" />
        <activity android:name=".presentation.ScheduleList" />
        <activity android:name=".presentation.ScheduleInterfaceController" />
        <activity android:name=".presentation.ProfileInterfaceController" />
        <activity android:name=".presentation.NotificationListFragment">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.SelectApp" />
        <activity android:name=".presentation.CreateProfileInterfaceController" />
        <activity android:name=".presentation.CreateScheduleInterfaceController" />
        <activity android:name=".presentation.SelectProfile"></activity>
    </application>

</manifest>